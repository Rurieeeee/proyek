data_sensor = []

def input_data():
    suhu = float(input("Masukkan suhu (°C): "))
    kelembapan = float(input("Masukkan kelembapan (%): "))

    data = {
        "suhu": suhu,
        "kelembapan": kelembapan
    }

    def tampilkan_status():
    print("Suhu dan kelembapan biji kopi normal")


input_data()
